// Example of a simple JavaScript function to calculate the sum of two numbers
function addNumbers(a, b) {
    return a + b;
}

// Example usage: adding 10 and 20
console.log(addNumbers(10, 20)); // Output: 30

// Another example with different variables
let result = addNumbers(5, 7);
console.log(result); // Output: 12
